{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "SqlServer": "Server(local); Database=UrlMapping; Trusted_Connection=true; Trust Server Certificate=true",
    "Sqlite": "Data Source=UrlMapping.db"
  }
}
